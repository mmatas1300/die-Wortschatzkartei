/** 
* @typedef User
* @prop {string} email  
* @prop {Array} myCards
* @prop {Object} config
* @prop {Array} streak
* @prop {Array} progress
*/

/**
 * @typedef Card
 * @property {?string} _id 
 * @property {string} type
 * @property {string} wort
 * @property {?string} plural 
 * @property {?string} manner
 * @property {?string} frau
 * @property {?string} frauen
 * @property {?string[]} prasens
 * @property {?string[]} prateritum
 * @property {?string} partizip2 
 * @property {?string} bild
 * @property {?string} verwandte
 * @property {?string} beispiel
 * @property {?string} ubersetzung
 */

export const Types = {}